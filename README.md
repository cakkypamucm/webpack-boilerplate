This is `webpack` boilerplate (`vue3`, `ts`, `scss`, `postcss`, `browserslist`, `babel`, `eslint`, `stylelint`, `prettier`)

### Development

```shell
npm start
```

### Building

```shell
npm run build
```

### Linting

```shell
npm run lint
```
